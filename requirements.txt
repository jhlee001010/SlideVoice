python-pptx
pymupdf
tqdm
coqui-tts
transformers==4.57.6  # coqui-tts가 transformers 5.x와 호환되지 않아 4.57 계열로 고정

# torch / torchaudio는 별도 설치하세요 (환경에 맞는 빌드를 선택해야 함):
#   GPU(NVIDIA, CUDA 12.4 드라이버 기준): pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
#   CPU만 사용:                          pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
